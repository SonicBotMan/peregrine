# M4-b1 R2 独立审查（delegate: reviewer, del_mu0w8emz_slrg）

结论：**无 P0；P1×1（已修复）；P2×4（2 修复 2 记录）** —— 可提交。

## P1（已修复）
- follow_live 轮询 `select!` 的 tick 分支可能与取消竞态：tick 胜出后直接 resolve，取消的任务会多发一次网络往返。修复：tick 唤醒后、resolve 前补 `cancel.is_cancelled()` 检查（lib.rs follow_live，标注 R2 P1）。

## P2（处置）
1. （修复）HlsError 需 pub re-export 供集成测试构造/匹配。
2. （修复）with_concurrency/with_poll_cadence_secs 的 `#[cfg(test)]` 使集成测试不可见——去除属性。
3. （记录）live 中途 EXT-X-KEY 更换（key 轮换）v1 不支持，merge 时报 "key vanished mid-merge"——诚实失败，backlog。
4. （记录）live + byterange 窗口滑动场景理论存在但无真实样本——RFC 允许，引擎按 per-segment range 处理，不额外覆盖。

## 覆盖确认
- ENDLIST 尾批必被下载后才 break（fresh 非空 + ended 分支）
- stall/取消/间歇 503/滑窗 gap 均有集成测试
- merge gap 检测文案与测试断言一致
