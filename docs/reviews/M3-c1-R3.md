# M3-c1 R3 — 终审反思

**R2 复审（del_mu01i2hs_kydd）处置**：基线验证通过（HEAD af2827e，diff 面正确）。五条发现：
- P0-1 采纳 ✅：后端推 `task_limit_changed`（bus.rs），前端 applyEvent 不消费 → 多窗口/恢复后 select 停在旧值。
  修复：types.ts EngineEvent 加变体 + store.svelte.ts 加 case（fold speed_limit_bps，未知 id → resync）。
- P0-2 采纳 ✅：throttle select 加 aria-label；chevron 加 aria-expanded（P2-5 合并）。
- P1-3 驳回：报告称「乐观置值不回滚」，实际 setTaskLimit 无乐观更新（等响应回填），失败时 select 自动弹回真值。
  遗留：失败静默无提示 → B39（统一错误 toast，M3-c2+）。
- P1-4 驳回：commitCustom 已有 Number.isFinite + >=0 守卫（TaskRow.svelte），NaN/空串不会发往 daemon。
- P2-5 采纳 ✅（并入 P0-2）。

验证：vite build 绿 + 全仓 cargo test 184 绿 + clippy 净。

## 本轮三层教训（流程层，比代码发现更有价值）

1. **压缩摘要不可信，交付物必须实证**：会话压缩块的摘要声称「M3-c1 前端全部写完、smoke 验证过」，
   实际前端一行未落盘（git status + grep 全零）。成因：长会话里「计划中的改动」与「已写入的改动」在压缩时被混写。
   对策（已执行）：恢复任何中断工作前先 `git status --short` + 关键符号 grep 实证清单，再动手。
2. **delegate 审查会基线幻觉**：R2 首轮（del_mu00ku35_wi7u）的正确发现（前端缺失）之外，
   引用了大量不存在的结构（SegmentView::from、problem()、migrations/003、端口 7879、HEAD 9817e47——真实 HEAD af2827e）。
   对策（已执行）：复审任务书强制 STEP 0 输出 `git rev-parse --short HEAD && git status --short && git diff --stat`，
   且报告 file:line 需抽查后才采纳。首份报告仅采纳「与真实代码兼容」的两条：in-flight 分数 pct 测试、(url,sink) 别名契约锁定。
3. **write/edit 相对路径（~ 开头）在本环境会静默失败**（报成功但文件不在磁盘）：format.ts/SegmentPanel.svelte/R1.md
   首写全丢。对策：一律绝对路径 + 落盘验证（ls/grep）。

## 代码层发现（R2 首轮 + 自查合并）

- in-flight 分数 pct 无回归锁 → 已补 `segments_pct_is_fractional_in_flight`（done=1/len=4 → 0.25）。
- (url,sink) 别名窗口（terminal 后 re-add）无契约锁 → 已补 `segments_alias_same_url_sink_shares_engine_row`
  （直接插行绕过守卫；守卫行为本身已有 add_duplicate_active_is_409 覆盖）。**别名语义**：两 wire 行共享同一 engine 行
  遥测——这是现状契约，不是缺陷；未来若要 per-task 遥测需 engine 行挂 wire id（v2.x）。
- duplicate_active 的「active」= 状态 NOT IN (completed, failed)——**包含 paused**。写测试时两次踩错（直觉以为 paused 可 re-add）。
  对策：契约已由两条测试（409 守卫 + 别名）双向钉死。

## 环境限制（入 BACKLOG）

- E2E 截图两次被环境挡：chromium headless 缺 libnspr4/libasound，无 sudo，apt 源不可用（apt-get download 也失败）。
  wire 层 curl smoke（8 段真实下载、limit/settings/404 全对）+ vite 代理链验证代替。
- 全局限速只在 UI 启动读一次（事件总线只带任务增量）——B37 家族已知限制。
